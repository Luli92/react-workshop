import React, { Component } from "react";
import "./App.css";

const button = () => {
  return (
    <div>
      <button onClick={testfunction}>Kommer från Button</button>;
    </div>
  );
};

export default button;
